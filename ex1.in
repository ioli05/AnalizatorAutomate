automata_language4 ::= FiniteAutomata (
	alphabet :: { a, b, c } ;
	states :: { q0, q1 } ;
	initial_state :: q0 ;
	final_states :: { q0 } ;
	variable y ::= { a, b, c } ;
	
	q0 -> (
		y -> q1 ;
	) ;

	q1 -> (
		y -> q0 ;
	) ;

) ;;